# Nadie accede a los archivos por HTTP directo.
# El único camino válido es serve.php, que valida sesión/token.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
